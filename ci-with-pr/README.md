# ci-with-pr

CI with PR creates a branch, commits and pushes changes, opens a draft pull request, waits for CI, fixes red builds, and iterates until the checks are green.

## Install

```bash
npx skills add https://github.com/yarlson/skills/tree/main/ci-with-pr
```
